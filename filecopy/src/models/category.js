class Category {
  constructor(id, title, image, searchPhrase) {
    this.id = id;
    this.title = title;
    this.image = image;
    this.searchPhrase = searchPhrase;
  }
}

export default Category;